version: "3.8"

services:
  geth:
    image: geth-local
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8545:8545"
      - "8546:8546"
    volumes:
      - geth-data:/root/.ethereum
    networks:
      geth-network:
        ipv4_address: 192.168.1.100
    environment:
      - WALLET_ADDRESS=
      - PRIVATE_KEY=

  scripts:
    image: geth-local
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      geth:
        condition: service_started
    volumes:
      - geth-data:/root/.ethereum
    networks:
      geth-network:
        ipv4_address: 192.168.1.101
    environment:
      - GETH_RPC_ENDPOINT=http://192.168.1.100:8545
      - GETH_WALLET_ADDRESS=0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266
      - GETH_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
      - CHAIN_ID=1337
      - VERSION=1
    entrypoint:
      ["sh", "-c", "sleep 30 && echo 'Geth node is healthy.' && ts-node /app/fund-wallet.ts && tail -f /dev/null"]

volumes:
  geth-data:

networks:
  geth-network:
    driver: bridge
    ipam:
      config:
        - subnet: 192.168.1.0/24
