import { SPACE } from "@nilfoundation/ui-kit";
import type { StyleObject } from "styletron-react";

const table: StyleObject = {
  width: "100%",
  flexGrow: 1,
  paddingTop: SPACE[16],
};

export const styles = {
  table,
};
